# svm_classification_kernel_methods_python
Applied Support Vector Machines (SVM) and logistic regression to solve binary classification problems using linear and non-linear decision boundaries. Performed hyperparameter tuning, cross-validation, and model comparison across kernels (linear, polynomial, RBF) to optimize predictive performance.
